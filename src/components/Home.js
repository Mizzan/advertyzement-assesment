import React from 'react';
import Header from './Header';
import HeroTitle from './HeroTitle';

const Home = () => {
  return (
    <div>
      <Header />
      <HeroTitle />
    </div>
  );
};

export default Home;
